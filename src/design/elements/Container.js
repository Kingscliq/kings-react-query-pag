import styled from "styled-components";
import {
	space,
	position,
	typography,
	color,
	border,
	display,
} from "styled-system";
const Container = styled.section`
	width: 968px;
	margin: auto;
	${space}
	${position}
	${typography}
	${color}
	${border}
	${display}
`;

export default Container;
