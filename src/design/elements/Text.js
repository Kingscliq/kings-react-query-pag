import styled from "styled-components";
import {
	space,
	position,
	typography,
	color,
	border,
	display,
} from "styled-system";

const Text = styled.p`
	${space}
	${position}
${typography}
${color}
${border}
${display}
`;

export default Text;
