import React from 'react'
import styled from 'styled-components'
import {color, typography, space, flexbox, layout, position, grid , border, background, shadow,  } from 'styled-system'

const Box = styled.div`
${color}

`



export default Box
